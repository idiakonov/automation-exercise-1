// <reference types="cypress" />

import { HeaderPage } from "../support/components/header";

describe('page header', () => {

    // const header = new HeaderPage();
    // it('should have page links with correct url', () => {
    //     cy.visit('/')
    //     header.getNavLinks().each(($el, index, $list) => {
    //         cy.wrap($el).invoke('attr', 'href')
    //             .then((href) => {
    //                 if (!!href.length) {
    //                     expect(header.headerLinkUrls.includes(href), `link with ${href} should exist`).to.be.true;
    //                 }
    //             })
    //     })
    // })
})